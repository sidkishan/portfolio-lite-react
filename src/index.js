import React from "react";
import ReactDOM from "react-dom";
import "./style.css"; //everything is finally coming here in index.js only. so all the styling will be applied here.
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
